/**
 * @file configs/responseCode.config.type.js
 * @description `responseCode.config.js`에서 사용하는 타입 정의 파일
 * 251119 v1.0.1 mastercat init
 */

/**
 * 응답 코드 설정 객체 타입 (`responseCode.config.js`에 정의된 상수만 사용)
 * @typedef {Object} ResponseCodeConfig
 * @property {string} code - 응답코드
 * @property {string} msg - 응답 메세지
 * @property {string} info - 응답 상세
 * @property {number} status - HTTP Status
 */

export {};